# astra-child

Ce dossier ne porte **que** ce que le chantier réservation Sexcape Room ajoute au thème enfant Astra réel : `inc/sexcaperoom-tunnel.php`, ses `assets/`, et l'unique ligne `require_once` à ajouter à `functions.php`. **Ce n'est pas une copie du thème réel** — le thème réel, actif sur linstantcle.ch depuis avant ce chantier, porte son propre `functions.php` (7390 octets, du suivi de conversion publicitaire, des règles `noindex` par page, la visibilité de la barre d'administration, etc.) et son propre `style.css` (2771 octets). Voir `docs/briefs/constat-deploiement-moteur.md` pour l'inventaire exact, établi par lecture directe du serveur le 20 septembre 2026, et **jamais écraser ces deux fichiers avec les copies de ce dossier**.

`style.css` et `functions.php` de ce dossier ne sont donc que des aide-mémoire de forme (l'en-tête minimal qu'un style.css de thème enfant doit porter ; le commentaire qui documente la ligne à ajouter) — pas des fichiers à déployer tels quels.
